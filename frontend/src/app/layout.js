export const metadata = {
  title: 'HanGan (หารกัน) - Trip Bill Splitter',
  description: 'Simplify trip expenses with friends without registering.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="th">
      <body>{children}</body>
    </html>
  )
}
